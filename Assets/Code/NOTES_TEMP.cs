using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NOTES_TEMP : MonoBehaviour
{/*
    1. Add trigger box player enters to move LvLs, it will switch the CamController obj
    2. Make the tutorial scene. 
    3. Cutscene Manager GameObject - 
        public class CutsceneManager : MonoBehaviour{
            public CamController camController; // ref the camera controller script
            public void StartCutscene()
            {
                foreach (GameObject cam in camController.cameras)
                {
                    cam.SetActive(false); // i havent tested this hope it works GAH
                }
            }
            public void EndCutscene()
            {
                // see this thing is to re-enable. but we might wanna switch floors after a cutscene most likely sooo yeah
                    its gonna be more complicated auuuaaa
                camController.cameras[camController.activeCameraIndex].SetActive(true);
            }
        }
    4. Remodel the 
    5. Thats all for now I think. goofnight

*/}
